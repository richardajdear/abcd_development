release: '5.1'
metric: thickness
parcellation: dsk
hemisphere: both
qc_policy: coded
sample_rule: min_visits
min_visits: 2
visits:
- v0
- v2
- v4
age_basis: linear
spline_df: 3
age_centre: null
re_structure: slope_correlated
global_covariate: none
covariates:
- sex
family_effect: false
site_effect: true
note: 5.1 matched to the settled 7.0 specification (no global covariate, no family
  random effect, >=2 visits). Exists so the cross-release power comparison in REPORT_7.0
  section 4.3 is matched on design; an unmatched 5.1 row previously made the >=3-visit
  filter look worse than 5.1.
